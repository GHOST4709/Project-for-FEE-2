/**
 * Google Maps JavaScript API key for map.html.
 *
 * Get a free key at https://console.cloud.google.com/apis/library
 * (search "Maps JavaScript API" and click Enable), then create a key under
 * APIs & Services -> Credentials. Restricting it to your site's origin is
 * recommended before you deploy this anywhere public.
 *
 * Leave this blank and map.html still works — it falls back to a styled,
 * to-scale static preview instead of the live map.
 */
window.BRIDGE_MAPS_API_KEY = "AIzaSyCsgr8M9YMLv4C_rph3718jBNhQyby57mo";
